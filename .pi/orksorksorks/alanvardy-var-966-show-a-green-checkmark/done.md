# Done

- **What was built**: In `CheckStitch/ContentView.swift`, the `createChecklistButton` now shows a rotating `ProgressView` to the right of the checklist name while its `Task { await createChecklistReminders() }` runs, swaps it for a green `checkmark.circle.fill` on completion, and auto-clears it after ~1s (`try? await Task.sleep(for: .seconds(1))`), returning the row to its resting state. Two `@State` flags (`isCreatingChecklist`, `isChecklistCreated`) drive the label's conditional trailing icon. No changes to `createChecklistReminders()`, `EditChecklistView`, or `MyApp.swift`.
- **Commit SHA(s)**: `16e3c20` "Show a green checkmark" (squashed from the worker's `3f7a9d5`; the stub commit `77444dd` was dropped and the branch rebased onto current `origin/main` so the PR diff contains only the `ContentView.swift` change).
- **Verification**: `bash scripts/test.sh` — `** BUILD SUCCEEDED **`, `gate: ok` (repo has no test target; the gate is a simulator build plus `shellcheck` over `scripts/`).
- **Reviewer findings**: No blockers. Three optional nits deferred: (1) the label re-centers ~15pt when the icon appears/disappears — a fixed-width icon slot would remove the jump; (2) the spinner/checkmark aren't announced to assistive tech; (3) rapid re-taps spawn overlapping Tasks that can transiently swap state (always settles back to resting).
- **Remaining manual items**: visual check on a simulator (spinner/checkmark rendering and the ~1s timing); PR #6 is still draft — mark ready and merge with `--rebase` when convenient; note the permission-denied path also ends in a brief green checkmark (spec reads "when done").