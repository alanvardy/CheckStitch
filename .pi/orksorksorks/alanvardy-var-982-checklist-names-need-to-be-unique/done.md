# Done

- **What was built**: CheckStitch now enforces unique checklist names. `ChecklistStore.create(name: "New checklist")` and `rename(id:to:)` reject any trimmed, case-insensitive duplicate (renames exclude the checklist being renamed), leaving no partial state; rejection sets an observable `store.nameConflict` that ContentView (create) and ChecklistDetailView (rename) surface via a SwiftUI `.alert("Name already in use", …)` with clear copy. Persistence format, debounce, and codec untouched.
- **Commit SHA(s)**: `2b5b974` — feat: reject duplicate checklist names on create and rename (also `git rm`s the `DELETEME` placeholder per its own instruction). Pushed to `origin/alanvardy-var-982-checklist-names-need-to-be-unique` (draft PR #23).
- **Verification**: `make test-unit` green, independently re-run by parent — 62 tests in 16 suites passed (CheckStitch app target compiles on macOS host + all unit suites). New tests cover duplicate default create (nil + count + reload), typed create success/duplicate (case/trim variants), rename success, self-rename allowed, rename-to-other rejected with in-memory and reload assertions. Full `bash scripts/test.sh` gate not run (scoped out for SMALL; UI smoke untouched).
- **Reviewer findings**: No blockers. Verdict: "OK with notes". P2 nits: (1) test file mixes `store.create()!` with the historical `try? XCTUnwrap(...)` idiom — legal (force-unwrap ban is outside tests only), flagged for standardization; (2) after a rejected rename the TextField can transiently show the rejected text until the store re-invalidates — cosmetic, alert explains; (3) alert title/message duplicated across the two views — accepted for two thin views.
- **Remaining manual items**: none required. Optional follow-ups: standardize test unwrap idiom, dedupe alert copy. Consider a UI smoke/manual pass on the simulator to eyeball alert dismissal behavior.