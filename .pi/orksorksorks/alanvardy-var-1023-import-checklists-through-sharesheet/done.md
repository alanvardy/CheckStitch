# Done

- **Branch / head SHA**: `alanvardy-var-1023-import-checklists-through-sharesheet` @ `0c9303444881492c6a97c1e4917189aa56242a54` (fixes applied after the user chose option [2]; pushed with `--force-with-lease`).
- **Rebase**: no conflicts. Working tree clean, no rebase in progress. Branch was already correctly rebased (8 ahead / 0 behind `origin/main`).
- **Mechanical checks**: `bash scripts/test.sh` printed **`gate: ok`** twice — once on the pre-fix head `5a2f82a`, and again after the review fixes: `make build` (simulator, warnings-as-errors) → pre-boot of this worktree's `.simulator_id` → `make test` → `make build-mac` → `make watch-build` → `bash scripts/tests/run.sh` (`tests: 25 passed, 0 failed`, incl. `documentTypeRegistrationWiresInfoPlist`) → `shellcheck`. `make test-unit` also green standalone: **378 tests in 49 suites** (up from 377 — the new design-pinning inbox test). No warnings surfaced as errors.
- **Review outcome**:
  - **Blockers: none.** Stage-without-writes, file-id selection, authoritative conflict re-check at commit, FIFO decisions, `@MainActor` funnel isolation, and security-scoped read all verified correct; test coverage is strong.
  - **Reviewer P1 (same URL re-shared after consume is dropped) — deferred as deliberate design, NOT changed.** `plan.md:137` explicitly keeps `lastReceivedURL` across `consume()` so a delivery's double-fire (scene + delegate) cannot re-open the sheet, and `implement.md` asserts the second Files-open must not double-import. The reviewer's suggested `consume()` reset would reintroduce the double-fire (the scene hook consumes before a possible delegate hook fires). Behaviour is now pinned by `sameURLAfterConsumeIsDroppedByDesign`. Revisit only if on-device testing shows a real re-share failure.
  - **Applied now**: removed dead `clearImportSelection()`; `importFile` clears the in-flight conflict only after a file successfully stages (a bad arrival no longer disturbs the open sheet / conflict); `commitImport` guards an empty selection; `ChecklistSelectionView` now takes `LocalizedStringKey` for title/confirm/row-detail, restoring localization lost by the shared-view refactor; added `Import Checklists` and `A checklist with this name exists` to the App catalog in all six languages and to `LocalizationFixtures.requiredKeys`; dropped the unused `@Observable` from `SharedImportInbox`; added the design-pinning inbox test; trailing newlines on new files.
  - **Not applied (declined)**: no remaining optional items; the macOS while-running consume path stays a manual verification item (see below).
  - **Verdict**: mergeable.
- **Remaining manual items (from `plan.md`/`implement.md`, unverifiable statically)**:
  - On-device iOS share-sheet flow: Mail attachment → Share → CheckStitch → selection screen; untick one → only ticked import; re-share same Mail attachment → sheet reappears.
  - Verify the **macOS while-running consume path**: `MacAppDelegate.application(_:open:)` calls `receive` but never consumes; if SwiftUI `.onOpenURL` does not also fire on macOS, a Finder "Open With" while running would sit pending until relaunch. Needs simulator/device validation.
  - `make run` plist regression check; plain `.json` → "Couldn't import" alert, no sheet; cold-start share yields exactly one sheet.
  - `bash scripts/run-devices.sh` installs + launches on the real iPhone and host Mac.