# Done

- **What was built**: Made the macOS CheckStitch window's close/quit lifecycle work per standard macOS convention and heal the root cause of the "close button does nothing" report. `MacAppDelegate` (thin `CheckStitch/` target, `#if os(macOS)`) now clamps any window whose frame intersects no display back onto the primary screen (pure, unit-tested `onScreenFrame` + empty-screen fallback), re-runs the idempotent clamp on launch/activation and on window move/resize/expose/update/key/close notifications, implements `applicationShouldTerminateAfterLastWindowClosed → false` (app stays active in the Dock) and `applicationShouldHandleReopen → true` (Dock click / `open` re-hosts the window), and `MyApp.swift` opts the macOS scene out of SwiftUI's persisted window-frame restoration (`restorationBehavior(.disabled)`) so a stale off-screen frame can no longer be replayed on launch. Cmd-W (close) and Cmd-Q (quit) keep their standard behavior. Everything is confined to `CheckStitch/` behind `#if os(macOS)`; `CheckStitchCore`, the shared scheme, and build settings are untouched.
- **Commit SHA(s)**: `a520b5f` (worker: clamp + lifecycle), `821e687`, `b43f4d6`, `6b4f9d0` (review fixes: trailing newline, `#if os(macOS)` gating of the test file — the simulator `test-ui` build otherwise fails to compile `NSRect` — empty-screen fallback, willClose re-clamp), `fdcb1e2` (scene restoration opt-out). Branch `alanvardy-var-979-close-button-doesnt-work`, PR #18 (draft).
- **Verification**: `./scripts/test.sh` gate → `gate: ok` (simulator build, `make test-unit` 29/29 across 10 suites including 6 new `MacWindowFrameTests`, `CheckStitchUITests` smoke, shellcheck). macOS app built unsigned and verified live with `osascript`/System Events: garbage persisted frame injected into the prefs → window heals on-screen; close-button click acknowledged ("button 1 of window CheckStitch") → window count 1→0, process stays alive; `open` re-opens a window; Cmd-W closes; Cmd-Q quits (process exits). The window follows the display array (built-in plus a DELL external above it — the window can land on the DELL, where the close button works identically).
- **Reviewer findings**: reviewer (pass 3) returned zero blockers, three P2 nits (doc-comment "primary-screen" wording, missing trailing newline, observer-lifetime note = no action). All fixed. A gate-time failure the reviewer could not see (`test-ui` compiles the test target for the iOS simulator where `NSRect` doesn't exist) was fixed by gating the test file `#if os(macOS)`.
- **Remaining manual items**: (1) The window position still follows macOS's remembered/display-array placement — on a machine where an external display was removed after the app last ran, a stale frame could still put the window out of sight until the app is activated; the clamp self-heals on activation and the scene opt-out prevents the persisted replay going forward. A human should launch the app once, confirm the window and close button, then close/quit to settle state. (2) PR #18 is draft — mark ready when convenient. (3) The full multi-monitor story (which display the window opens on) is macOS state, not app logic.