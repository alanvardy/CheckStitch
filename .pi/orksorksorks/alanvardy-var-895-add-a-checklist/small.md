# Task

Add a centered button called "checklist" that, when tapped, creates three reminders in the Reminders inbox named "one", "two", and "three".

## Why SMALL

Single-module UI change (button + reminder creation) with clear spec, no schema changes, no new subsystems, and localized tests.

## Key files (if the recon found any)

- UI component for the checklist button
- Reminder creation service/function
- Reminder inbox view (to verify reminders appear)
