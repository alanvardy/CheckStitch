# Findings — VAR-1076 test-suite audit

Things that could not be honestly unit-tested (or could not be tested headlessly) in this
slice, with severity + evidence. Produced against the committed state of
`alanvardy-var-1076-test-suite-audit` after phases 1–5.

Nothing listed here was silently "fixed" — every item is either a deliberate design
decision, an untestable seam, or an unforceable branch, recorded rather than refactored.

---

| # | Finding | Severity |
|---|---|---|
| 1 | `PhoneSyncAdapter` / `StoreKitPurchaseService` untested: real iCloud/StoreKit transports, not headlessly drivable; the `ChecklistSyncing` / `PurchaseProviding` seams carry their behaviour.<br>Evidence: `CheckStitch/PhoneSyncAdapter.swift:11` (`final class PhoneSyncAdapter: NSObject, ChecklistSyncTransport`) — WCSession delegate methods (`session(_:activationDidCompleteWith:...)`, etc.); `CheckStitch/StoreKitPurchaseService.swift:7` (`final class StoreKitPurchaseService: PurchaseProviding`) — `offer()/purchase()/restore()` hit StoreKit 2. Neither can run headless. | medium |
| 2 | `AppGroup.defaults` `?? .standard` fallback is unforceable in the signed macOS test host; only reachability pinned, not the branch.<br>Evidence: `CheckStitchTests/AppGroupTests.swift` — `defaultsIsUsableWhenSuiteUnavailable` asserts reachability only; the `?? .standard` branch (design Open Risk 2) cannot be forced because the suite namespace resolves in the test host. | low |
| 3 | `ChecklistSyncDiagnostics.appendToDisk` (Documents write, lock, reset-on-disk) has no headless test — only `record` / `shouldResetFile` are covered. It depends on `FileManager.urls(for: .documentDirectory, ...)`, a serial `diskLock`, and on-disk state.<br>Evidence: `CheckStitchCore/Sources/CheckStitchCore/ChecklistSyncDiagnostics.swift:68` (`private static func appendToDisk(_ line: String)`, `diskLock.lock()`, Documents `FileHandle` write, reset-on-disk); covered surface is `record`/`shouldResetFile` only. | low |
| 4 | `ChecklistExportDocument.init(configuration:)` / `fileWrapper(configuration:)` cannot be unit-tested: `FileDocumentReadConfiguration` / `FileDocumentWriteConfiguration` have no public initializer (SwiftUI interface). The encoder paths behind `init(checklists:)` + `data` are already covered by `ChecklistExportTests.testFileWrapperCarriesEncodedBytes`; only `readableContentTypes` is newly pinned.<br>Evidence: `CheckStitch/ChecklistExportDocument.swift:17,24`; `CheckStitchTests/ChecklistExportTests.swift:104`; `CheckStitchTests/ChecklistExportDocumentTests.swift`. | medium |
| 5 | `AppEnvironment` has no production construction site and no default `reminderCreator`; likely dead/unwired code.<br>Evidence: `CheckStitchCore/Sources/CheckStitchCore/Environment.swift:5-10` — `public struct AppEnvironment: Sendable`, `init(reminderCreator:)` with no default; `rg AppEnvironment CheckStitch CheckStitchCore` → definition only. Pinned as a contract test (`CheckStitchTests/AppEnvironmentTests.swift`), not a default-path test. | medium |
| 6 | `ChecklistSyncing` has no direct behavioural test of a production conformer: `UbiquitousChecklistSync` is a construction/cancel canary only, and `NSUbiquitousKeyValueStore` cannot be substituted with a fake (no public init).<br>Evidence: `CheckStitchCore/Sources/CheckStitchCore/ChecklistSyncing.swift:22-23` (`final class UbiquitousChecklistSync`, `init(store:)` takes the concrete store); `CheckStitchTests/UbiquitousChecklistSyncTests.swift:10,16` (construction + cancel-idempotence only); `CheckStitchTests/ChecklistSyncingContractTests.swift` pins the shared `InMemoryChecklistSync` fixture instead. | medium |
| 7 | `AppearanceViewModel` is stateless delegate forwarding (no preference store); its `MacAppDelegate` / `AppDelegate` effects (`NSApp.windows` / `UIWindow`) are not assertable in a unit test without a new seam. Preference persistence is already covered by existing preference suites.<br>Evidence: `CheckStitch/AppearanceViewModel.swift:7` (stateless `@Observable` forwarder, no prefs stored); `CheckStitchTests/AppearanceModePreferenceTests.swift`, `CheckStitchTests/OrientationPreferenceTests.swift`. | low |
| 8 | `ContentView.perform(_:)`'s two dispatch lines (`beginExport` / `beginImport`) remain untested; only the route mapping and the VM entry points are covered (behaviour-preserving refactor deliberately not over-tested).<br>Evidence: `CheckStitch/ContentView.swift:594-597`; covered by `CheckStitchTests/ContentViewSettingsActionTests.swift` (`routeMapsExport`/`routeMapsImportChecklists`/`stagedActionSurvivesTakeAndRoutes`). | low |
| 9 | `Color.systemBackground`'s iOS/watchOS branch is not compiled in the macOS-hosted suite; only the macOS branch is asserted. The `#else` branch (`Color(uiColor: .systemBackground)`) compiles only under the iOS/watchOS SDK.<br>Evidence: `CheckStitch/Color+CrossPlatform.swift:15-21`; asserted in `CheckStitchTests/ColorCrossPlatformTests.swift` against `NSColor.windowBackgroundColor` (macOS branch only). | low |
| 10 | Duplicate fakes: none found — `TestFixtures.swift` already covers every fake needed (negative result). `SpyReminderCreator`, `InMemoryChecklistSync`, `makeIsolatedDefaults`, `makeItem` all reused; no new fake added. | — |