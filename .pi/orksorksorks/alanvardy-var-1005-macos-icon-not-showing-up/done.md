# Done

- **What was built**: Added a macOS app icon to the shared `CheckStitch/Assets.xcassets/AppIcon.appiconset/Contents.json` — the classic 10-entry mac idiom ladder (16/32/128/256/512 @1x/2x, 10 PNGs generated from the existing 1024×1024 `AppIcon.png` via `sips`) — so the macOS slice of the CheckStitch target no longer compiles a generic icon. iOS and watchOS (`CheckStitchWatch`, separate catalog) untouched. The modern single-size mac entry (`idiom: mac, platform: macos`) was tried first and **rejected by actool on this toolchain** ("unassigned child" warning, no icns) — hence the ladder fallback, which is the pre-authorized plan.
- **Commit SHA(s)**: `0b8d69e21ec543ed7e82c4caf9ec77e10e15f0c8` (`fix: add macOS app icon ladder to shared AppIcon asset catalog`), pushed to `origin/alanvardy-var-1005-macos-icon-not-showing-up`. Draft PR #42 attached.
- **Verification**: `make build-mac` BUILD SUCCEEDED and the compiled `CheckStitch.app` now carries `Contents/Resources/AppIcon.icns` (97 KB), `CFBundleIconFile`/`CFBundleIconName` = AppIcon in Info.plist, and all 10 `AppIcon` renditions in `Assets.car` (all absent pre-fix). `make build` (iOS sim) and `make watch-build` BUILD SUCCEEDED. Full gate `bash scripts/test.sh` → `gate: ok` (17 shell tests passed). All 10 ladder PNGs verified at exact size×scale dimensions.
- **Reviewer findings**: no blockers, no material nits. Noted only: mac `icon_*` naming differs from the watch catalog's `Icon-*` naming but is Xcode's standard mac naming (independent catalogs — acceptable); mac entries appended after iOS entry is cosmetic. All reviewer residuals closed by the parent (dimensions, compiled output, commit scope).
- **Remaining manual items**: (1) `DELETEME` placeholder is deleted in the working tree — `git rm`/include it before merging PR #42; (2) human spot-check the Finder/Dock icon via `make build-mac-signed` (signed, runnable build — the unsigned gate leg can't show it in Finder/Dock); (3) mark PR #42 ready and merge (`gh pr merge 42 --rebase --delete-branch`).