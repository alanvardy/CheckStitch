# Done

- **Branch / head SHA**: `alanvardy-var-977-add-tests` — code head `da4d736` ("review: drop placeholders, cover access errors, tidy test/build hygiene"), pushed to `origin` (fast-forward from `d8d3bb2`). This `done.md` is committed on top as the pipeline's closing artifact.
- **Mechanical checks**: `bash scripts/test.sh` → `gate: ok` — simulator build, macOS-hosted `CheckStitchTests` (23/23 passing, verified via `xcresulttool`), one `CheckStitchUITests` accessibility smoke on this worktree's `.simulator_id` simulator, and `shellcheck scripts/*.sh`. No blocking warnings.
- **Review outcome**:
  - Two-reviewer fan-out (production-core angle + tests/infra angle) over the full code diff.
  - **Blocker fixed**: removed the tracked `DELETEME` placeholder.
  - **Fix applied**: deleted stale `CheckStitchCore/Sources/CheckStitchCore/Placeholder.swift`; added `ChecklistCreatorTests.accessErrorReturnsFailedWithoutCreating` (locks the `guard try` → `catch` → `.failed` path); trimmed `EventKitReminderCreatorTests` to a real-adapter construction canary (the previous three cases exercised `SpyReminderCreator`, not the adapter, and one assertion was a tautological type-name string); renamed `ViewRenderTests.contentViewBodyEvaluates` → `contentViewBodyRendersNonEmpty`; set scheme testables `parallelizable = "NO"`; normalized missing trailing newlines across the new files.
  - **Rejected reviewer finding**: the claimed P1 that `ChecklistCreator.swift:23` (`guard try await reminders.requestAccess() else { return .permissionDenied }`) swallows thrown access errors. Verified with a compiled minimal repro that a thrown error propagates to the outer `catch` and yields `.failed(...)`, not `.permissionDenied`; behaviour is correct, only the test gap remained (now covered).
  - **Deferred as behaviour parity with `main`** (not regressions, out of scope): partial-creation `.failed` discards the created-so-far count; `.created(count: 0)` still flashes the green check when every row is blank.
- **Remaining manual items**: the plan's manual verification checklist (`.pi/orksorksorks/alanvardy-var-977-add-tests/implement.md` → "Manual Verification Items"): `make run` interaction checks (spinner ≥1 s, checkmark flash, Reminders permission prompt/deny, Settings light/dark/system round-trip on device, Edit-checklist add/rename/reorder/delete with blank-row skip). None are automatable in CI; all are unchanged behaviour from `main`.
