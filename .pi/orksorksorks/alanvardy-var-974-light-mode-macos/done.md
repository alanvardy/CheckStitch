# Done

- **What was built**: Fixed VAR-974 (macOS Light mode did nothing) by threading `AppearanceMode.colorScheme` into the SwiftUI canvas via macOS-gated `.preferredColorScheme(appearanceMode.colorScheme)` on `ContentView` and `SettingsView`. The macOS SwiftUI canvas does not honor the window-level `NSWindow.appearance` bridge (CheckStitch's `MacAppDelegate` is behaviourally identical to the SingleThread reference), so the canvas — the only macOS channel proven to work via the SettingsView dark preview — now repaints live on picker change. The window bridge, iOS `overrideUserInterfaceStyle` path, and `.system → nil` semantics are untouched.
- **Commit SHA(s)**: `2db59b3` (`fix: drive macOS canvas appearance via preferredColorScheme`), pushed to `origin/alanvardy-var-974-light-mode-macos` (draft PR #13).
- **Verification**: `bash scripts/test.sh` → `** BUILD SUCCEEDED **` + `gate: ok` (iOS simulator build + shellcheck). Manual macOS compile check `xcodebuild -scheme CheckStitch -destination 'platform=macOS' -configuration Debug -derivedDataPath DerivedData CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **` (this compiles the changed `#if os(macOS)` lines, which the iOS-only gate never sees). The changed macOS-gated lines are NOT covered by the repo gate — the macOS build is the required keep-green command.
- **Reviewer findings**: No blockers, no nits (reviewer pass 6e971b9d). Root cause, `.system` semantics, iOS regression risk, sheet-window threading justification, and conventions all checked and found correct.
- **Remaining manual items**: Exercise Light/Dark/System live on a real macOS machine (headless verification of the actual GUI flip isn't possible from here). Confirm the Settings sheet flips live alongside the main window. `DELETEME` was left deleted-but-unstaged in the worktree, per small.md (exclude from the change).