# Done

- **What was built**: Constrained the iPad "edit checklist" screen (`ChecklistDetailView.swift`) and its item subscreen (`ItemEditView.swift`) to a centered narrow column, mirroring the main list's `ChecklistWidth.maxContentWidth` cap (`min(340, viewportWidth*0.6)`). Each view's `body` is wrapped in a `GeometryReader`, and its `Form`/`Group` gets `.frame(maxWidth: ChecklistWidth.maxContentWidth(viewportWidth: geometry.size.width), alignment: .center)` — iPad gets the Settings-style narrow centered pane; iPhone is effectively unaffected (`min(340, ...)` hugs narrow screens).
- **Commit SHA(s)**: `55b7069` — "narrow checklist-edit screens to a centered column on iPad" (pushed to `origin/alanvardy-var-1050-edit-checklist-needs-to-be-narrower-on-ipad`)
- **Verification**: `make build` (simulator, WARNINGS_AS_ERRORS) succeeded; `make test-unit` passed (361 tests / 48 suites); full `bash scripts/test.sh` gate passed (`gate: ok`, including watch-build, build-mac, and 24/24 shell tests).
- **Reviewer findings**: No blockers. Three optional P2 notes deferred: (1) transient "not found" fallback is uncapped in `ChecklistDetailView` but capped in `ItemEditView` — matches the main list's uncapped empty-state precedent, so left as-is; (2) the `ChecklistWidth` used is the app-module enum (identical `min(340, ...)` math) rather than the Core public type — no functional impact (non-issue); (3) the new `.frame(...)` lines are ~114 chars (no line-length rule).
- **Remaining manual items**: Visual confirmation on a real iPad/wide simulator that the edit-checklist and item-edit screens now hug a centered narrow column like Settings (SwiftUI layout is compiler-verified per repo convention; a quick on-target look is the only human check left).