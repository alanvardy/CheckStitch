# Done

- **What was built**: `scripts/run-devices.sh` now also installs + launches on the paired Apple Watch — a new watch leg after the macOS step shells out to the existing `scripts/run-watch.sh` (single source of truth for name resolution incl. typographic-apostrophe/NFKC folding, watchOS build, devicectl install + launch with `app.alanvardy.CheckStitch.watchkitapp`) and tallies failures into the script's exit status, so an unpaired/unreachable watch warns and fails the run without breaking the iOS/mac legs. Added `RUN_WATCH` override (default `1`, mirroring `RUN_MAC`); success summary gains "... and the Apple Watch". Discovery/macOS behavior stays byte-compatible. `scripts/tests/run.sh` gained 5 `run_devices_watch_*` cases mirroring the `run_watch_*` fixture pattern; `AGENTS.md` run-devices.sh bullet now documents watch coverage + `RUN_WATCH=0`. The separate `scripts/run-watch.sh` is unchanged for direct single-device use.
- **Commit SHA(s)**: `4586d48` (feat: install and launch on the Apple Watch from run-devices.sh), `d2077b2` (review: align no-devices note and unknown-watch test with watch leg), plus the implementation-summary and this done.md commit. SHAs are post-rebase; see `git log origin/main..HEAD`.
- **Verification**: Full gate `./scripts/test.sh` prints `gate: ok` — simulator build, `make test` (300 unit tests in 36 suites + 1 UI smoke), `make build-mac`, `make watch-build`, 22/22 shell tests (incl. 4 pre-existing `run_watch_*` + 5 new `run_devices_watch_*`), `shellcheck`. `make test-unit` alone → 300 passed.
- **Pre-existing main breakage unblocked**: `run-devices.sh` initially failed at the iOS build (which embeds the watch app) with `ChecklistSync.swift:29: switch must be exhaustive`. `RunResultKind.init(_ outcome:)` never handled `ReminderRunOutcome.partiallyCreated` (added by VAR-1019, `b4eb0cd`; the missing arm came from VAR-1027, `65ba443`); present unchanged on `origin/main`, so `main` was red for every fresh build. While this branch was in review, `main` landed the canonical fix (`13055f3`, adding `.partiallyCreated(created:total:)`, a `RunPhase.partiallyCreated`, the `total` wire key, and tests). This branch was rebased onto it and its own duplicate fix was dropped; all `*.swift` here are now byte-identical to `origin/main`. The green gate above is post-rebase.
- **Reviewer findings**: 0 blockers. 4 nits; fixed 2 trivially (no-devices note/header now mention the watch leg; unknown-watch test now asserts the macOS leg still ran). Deferred: summary string "on 0 device(s) and macOS and the Apple Watch" is grammatically odd with zero devices (matches pre-existing "and macOS" style); `RUN_WATCH=yes` would trip `[[ -eq ]]` under `set -e` (identical pre-existing `RUN_MAC` behavior, not a regression).
- **Remaining manual items**: optionally spot-check the real-watch path with a paired watch (`bash scripts/run-devices.sh`, or `RUN_WATCH=0` to skip); the fix's "3 of 7" partial-creation line is only observable on-device. The `DELETEME` deletion and this `.pi/` dir are branch-start scaffolding/step artifacts, not ticket work.