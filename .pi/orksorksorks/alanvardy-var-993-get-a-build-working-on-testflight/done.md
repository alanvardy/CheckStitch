# Done

- **Branch / head SHA**: `alanvardy-var-993-get-a-build-working-on-testflight` / `addba81` (pushed to `origin`; `3859ea3..addba81`)
- **Mechanical checks**: `bash scripts/test.sh` → `gate: ok` (17 shell tests passed), run after the review fixes. Diff is documentation-only: no `Makefile`, `scripts/`, `project.pbxproj`, `*.entitlements`, or scheme changes, so the gate and all local dev targets are unaffected. Rebase: no conflicts; working tree clean.
- **Review outcome**: One fresh-context reviewer plus a parent cross-scan. **No blockers.** All repo-verifiable claims in `docs/TestFlight-xcode-cloud.md` checked out (bundle ids, `MARKETING_VERSION 1.0` / `CURRENT_PROJECT_VERSION 1`, `IPHONEOS_DEPLOYMENT_TARGET 18.7` / `WATCHOS_DEPLOYMENT_TARGET 26.0` / `MACOSX_DEPLOYMENT_TARGET 27.0`, App Group + KVS entitlement set, `ArchiveAction = Release`, watch `Embed Watch Content`, no `.github/` or `ci_scripts/`, 17 shell tests).
  - **Fixes applied (worth doing now)**: (1) `conventions.md` shell-suite count `11 cases` → `17 cases` and line range `18-223` → `1-311`; (2) `conventions.md` typo `pkxproj:497-498` → `pbxproj:497-498` and stale sandbox-check range `run.sh:202-220` → `282-308`; (3) runbook "must match … exactly" reworded to "must include exactly what … declares (a superset is fine, never a subset)" to match `phase0-portal.md`.
  - **Optional improvements applied**: (4) runbook "Invalid pre-release train" now notes this ticket shipped no `1.0` train (future-operations note); (5) trailing newline added to all 13 `.md` files in the diff.
  - **Declined / deferred**: none — nothing in the review was rejected.
- **Remaining manual items**: the ticket's actual deliverable (an installable TestFlight build) is **owner-pending external state** — Phases 0–2 stay `_pending_` in `phase0-portal.md` / `phase1-archive.md` / `phase2-testflight.md`. The owner must complete the portal/ASC/Xcode Cloud/device steps and fill those tables before this branch is merged. Accepted residual risk: no committed regression signal for the Xcode Cloud workflow (documented in the runbook's "Residual risk (accepted)"). macOS TestFlight is a deferred fast-follow (separate ticket).