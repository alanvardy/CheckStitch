# Done

- **Branch / head SHA**: `alanvardy-var-994-macos-build-shows-no-background-photo-works-on-iphoneipad` — reviewed code head `6506bee`; review fixes and this marker committed on top (rebased on `main` @ `7aacadf`; working tree clean; local == `origin/*`; no rebase conflicts)
- **Mechanical checks**: `bash scripts/test.sh` → `gate: ok` after the review fixes (simulator build, headless pre-boot, unit + UI smoke, unsigned macOS leg, watchOS leg, `scripts/tests/run.sh` 17 passed / 0 failed including `macos_slice_requests_outgoing_network`, `shellcheck` clean). No warnings beyond the pre-existing watchOS AppIntents "Metadata extraction skipped" note. No warnings beyond the pre-existing watchOS AppIntents "Metadata extraction skipped" note.
- **Review outcome**:
  - Diff vs `main`: `CheckStitch.xcodeproj/project.pbxproj` (+2: `ENABLE_OUTGOING_NETWORK_CONNECTIONS = YES;` in the app target's Debug and Release configs), `scripts/tests/run.sh` (+18: regression pin), plus pipeline artifacts.
  - One bounded fresh-context reviewer, five adapted angles (fix correctness, entitlement placement, pin robustness, shell correctness, docs). **No blockers.**
  - Fix correctness confirmed: `ENABLE_APP_SANDBOX = YES;` occurs exactly twice (lines 497, 542), both in the `CheckStitch` app target's Debug/Release blocks for `SUPPORTED_PLATFORMS = "iphoneos iphonesimulator macosx"`; the watch target and test targets are not sandboxed. Entitlement belongs in the build settings (which synthesize `com.apple.security.network.client`) and *not* in the shared `CheckStitch/AppGroup.entitlements`, which is code-signed into all three SDK slices.
  - **Fixes applied (option [2])**: replaced the whole-file count-equality pin with an `awk` brace-depth walk (`scripts/tests/run.sh`) asserting every `buildSettings` block that sets `ENABLE_APP_SANDBOX = YES;` also sets `ENABLE_OUTGOING_NETWORK_CONNECTIONS = YES;`, and that at least one sandboxed block exists. This closes both P2s (per-block co-location, clean failure when the pbx is missing) and makes the comment's invariant accurate. Mutation-tested: current tree pass; Release egress removed fail; new sandboxed block without egress fail; egress reordered earlier in the same block pass; no sandboxed block fail; egress moved to a different block fail; new sandboxed block with egress pass.
  - **Declined optional nit**: `[sdk=macosx*]`-qualifying the egress/build-sandbox lines — reviewer also marked no change needed; it would diverge from the file's existing unconditional `ENABLE_APP_SANDBOX`/`ENABLE_USER_SELECTED_FILES` convention and the setting is inert on the non-sandboxed iOS slices.
- **Remaining manual items** (from `plan.md` / `implement.md`, unchanged by this review):
  - [ ] Signed launch + Settings → **Refresh wallpaper** with `log stream --predicate 'subsystem == "app.alanvardy.CheckStitch"'` running: no `Background force refresh failed`, and the photo renders (default 50 and faded opacity, correct across window resizes).
  - [ ] `make run` on the simulator: iOS rendering unchanged (iOS received no code change).