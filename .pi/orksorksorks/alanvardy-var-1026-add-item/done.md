# Done

- **What was built**: Tapping "Add Item" on the checklist detail screen now prompts for the item's name in an alert (mirroring the Duplicate Checklist pattern) instead of immediately creating an item titled "New item"; the item is created once with the typed name on the confirm button, via a new title-carrying `addItem(to:title:)` store create (old signature kept as a delegating overload, so no double revision/clock bump).
- **Commit SHA(s)**: `93f54cb7d776b3c782e8a97c0d33b454d51867c2` (feat: prompt for the item name when adding an item) — pushed to `origin/alanvardy-var-1026-add-item`.
- **Verification**: `make test-unit` — 237 tests in 33 suites passed. New tests `addItemIsGatedBehindANameAlert` and `testAddItemWithTitleCreatesOnceUnderThatTitle` confirmed green, as is the guarded `testCreateAddAndDuplicateSeedEveryFieldClock`.
- **Reviewer findings**: No blockers. Two P2 nits deferred: (1) "no create before confirm" is only structurally pinned (button action sets state only; the body cannot be staged headless, same constraint as the duplicate-flow precedent); (2) confirming with a blank name passes an empty title through — cosmetic, already tolerated by `ItemRow.displayTitle` fallback and out of ticket scope.
- **Remaining manual items**: None for the change itself. PR #46 is still a **draft** — mark it ready for review. The pre-existing working-tree deletion of `DELETEME` was intentionally left out of the commit; `.pi/orksorksorks/…` artifacts remain untracked (they are committed per repo convention if desired). Full `bash scripts/test.sh` gate (build-mac + watch-build + UI smoke) not run — targeted `make test-unit` only, per process.