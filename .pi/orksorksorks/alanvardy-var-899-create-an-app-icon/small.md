# Task

Add a full App Store icon set to CheckStitch, mirroring SingleThread's already-shipped
version (Linear VAR-574 / SingleThread commit `226a3087`, and its current
`AppIcon.appiconset/Contents.json`). The shipped icon artwork already exists at
`/Users/vardy/Downloads/checkstitch.png` (1254×1254 RGBA PNG); it must be wired into
the Xcode project so every supported device family (iOS phone/tablet + macOS — CheckStitch
has **no** Watch target) builds and launches with the icon, and the project keeps
building and passing the gate.

## Why SMALL

All A–F hold: (A) single module — the app target's already-existing
`CheckStitch/Assets.xcassets` asset catalog; the proven reference change is 2 files,
≤~5 here; PBXFileSystemSynchronizedRootGroup means no `project.pbxproj` edits for new
files. (B) 0–2 unknowns — source art provided and the exact recipe (single 1024×1024
master `AppIcon.png` + trimmed `Contents.json` with one `universal`/`ios` entry) is a
copy of SingleThread's landed commit; no new tech, no spike. (C) no schema/migration.
(D) no new subsystem or integration — the catalog and its convention already exist
(`AccentColor.colorset` sibling); no shared/CI/format code touched. (E) no design
decision — the icon graphic is finished; this is mechanical wiring. (F) no tests
needed — assets aren't covered by `CheckStitchTests`/UI suites, and `make build`
validates the catalog.

## Key files

- `/Users/vardy/Downloads/checkstitch.png` — source artwork (1254×1254; master
  needs to be the single required size, e.g. 1024×1024 as SingleThread used).
- `CheckStitch/Assets.xcassets/AppIcon.appiconset/` — **new** directory:
  `AppIcon.png` (master) + `Contents.json` (single `universal` image entry; the
  remaining per-size PNGs are generated by Xcode from the master — SingleThread's
  iOS catalog contains only these two files today).
- Reference to copy verbatim:
  `git -C /Users/vardy/dev/SingleThread show 226a3087` and read the current
  `/Users/vardy/dev/SingleThread/SingleThread/Assets.xcassets/AppIcon.appiconset/Contents.json`.
  The iOS commit changed exactly 2 files (added `AppIcon.png`, trimmed `Contents.json`
  from the old multi-size listing).
- Cleanup: the branch start commit carries a stray `DELETEME` file — remove it
  before the PR.
- Verify with `make build` (validates the appiconset parses and the icon is applied);
  the app launches with the icon via `make run` on the worktree's simulator.