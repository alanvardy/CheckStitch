# Done

- **What was built**: Replaced the single-device iPhone flow in `scripts/run-devices.sh` with the SingleThread shape — discovers ALL Developer-Mode-enabled, reachable iPhone/iPad devices (python3 `devicectl` parsing, unreachable devices tallied via `UNREACHABLE_LOG`), installs + launches on each, then (default `RUN_MAC=1`) builds `platform=macOS` unsigned (`CODE_SIGNING_ALLOWED=NO`) and `open`s the built `.app`. Non-zero exit with a failure-count summary on any failed step; no-device cases behave per the reference. Added `macosx` to `SUPPORTED_PLATFORMS` in both PBXNativeTarget build-settings blocks of `CheckStitch.xcodeproj/project.pbxproj`. All CheckStitch conventions preserved (`#!/bin/bash` + `set -euo pipefail`, mode 100755, `SCHEME`/`BUNDLE_ID`/`CONFIGURATION`/`DERIVED_DATA`/`RUN_MAC` overrides, `-allowProvisioningUpdates` on the iOS build, no bare `name=` destination, no test target added).
- **Commit SHA(s)**: `c6bec9e` (implementation), `61ec9a7` (nit fix: header-comment env-override wording), `b4ab45c` (macOS build fix: `navigationBarTitleDisplayMode` → `toolbarTitleDisplayMode`)
- **Verification**: `bash scripts/test.sh` → `** BUILD SUCCEEDED **` + `gate: ok` (shellcheck clean), exit 0; `bash -n` ok; mode verified `100755` in git; all pushed (branch in sync with `origin/alanvardy-var-894-run-on-ipad-and-mac`). **MacOS build now verified**: `xcodebuild -scheme CheckStitch -destination 'platform=macOS' … CODE_SIGNING_ALLOWED=NO build` → `** BUILD SUCCEEDED **` (only the benign `MACOSX_DEPLOYMENT_TARGET=27.0` warning — host runs macOS 27.0); `.app` present at `DerivedData/Build/Products/Debug/CheckStitch.app`. The `navigationBarTitleDisplayMode` compile error (iOS-only API surfaced by adding `macosx` to `SUPPORTED_PLATFORMS`) was fixed in `b4ab45c`. No test target exists in this repo — the gate is build + shellcheck, per AGENTS.md.
- **Reviewer findings**: reviewer agent could not launch (OpenRouter API key unavailable at review time) — review done by parent. **No blockers.** One nit (header comment referenced `scripts/test.sh` which has no env overrides) — fixed in `61ec9a7`. No unresolved findings.
- **Remaining manual items**: 
  - Full `./scripts/run-devices.sh` smoke test is now the only unverified path — the user's own `r` run already proved device discovery/install/launch on both iPhone and iPad, and the macOS build succeeds; the remaining unknown is the unsigned macOS app opening on this host.
  - Pre-existing, unrelated: branch-start placeholder `DELETEME` has an unstaged working-tree deletion left alone intentionally; `.pi/orksorksorks/` artifacts are untracked (both predate this change).