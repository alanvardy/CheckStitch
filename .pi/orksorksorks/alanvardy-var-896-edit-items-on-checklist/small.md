# Task

Edit the checklist button in ContentView.swift to:
1. Make it larger with an outline border
2. Add an icon-based "Edit" button next to it
3. When clicking the edit button, show an overlay allowing users to:
   - Change the checklist name
   - Add, remove, and edit items in the checklist

## Why SMALL

Single-file UI change (ContentView.swift only); no schema/API changes; follows existing SwiftUI patterns; local testing sufficient.

## Key files (if the recon found any)
- `CheckStitch/ContentView.swift` - main file to modify