# Done

- **What was built**: A settings menu for CheckStitch — a gear button (SF Symbol `gearshape`, top-left of `ContentView`) opens a `SettingsView` modal sheet with a single appearance entry (System/Light/Dark picker). `AppearanceMode` + `AppearanceModePreference` (UserDefaults, shared `"appearanceMode"` key) were ported from SingleThread as-is; the theme applies at the iOS window level via a new `AppDelegate` (`applicationDidBecomeActive` → `applyAppearance` → `UIWindow.overrideUserInterfaceStyle`; `.system` → `.unspecified` clears the override). Persisted through `@AppStorage("appearanceMode")`; `.system` defaults on missing/invalid values.
- **Commit SHA(s)**: `2bc85b3` "Add settings menu with appearance theme" (5 files, +233; pushed to `origin/alanvardy-var-970-add-settings-menu`).
- **Verification**: `make build` succeeded; full gate `./scripts/test.sh` ran green (`BUILD SUCCEEDED` + shellcheck ok; CheckStitch has no test target).
- **Reviewer findings**: The `reviewer` (and `delegate`) agent could not launch — openrouter provider key expired (`No API key found for openrouter`) — so per the SMALL fallback rule the review pass was performed in-process against `small.md` and the SingleThread reference. **No blockers**. Nits (all deferred/consistent with house style): gear button omits SingleThread's `accessibilityAddTraits(.isButton)` (matches CheckStitch's existing `editChecklistButton`); `AppearanceMode.swift` carries the macOS branch/comments that compile out on iOS (faithful as-is port); caption uses a plain `Text` in place of SingleThread's `SettingsCaption` (not present in CheckStitch).
- **Remaining manual items**: Visual QA on a device/simulator (`make run`) — tap the gear, switch Light/Dark/System, confirm the theme overrides the whole window and survives relaunch. No functional issues known; build-only verification was performed.