# actionlint's built-in runner-label list predates GitHub's `xcode-27`
# preview image (actions/runner-images#14404), so declare it here to keep
# `actionlint .github/workflows/` green.
self-hosted-runner:
  labels:
    - xcode-27